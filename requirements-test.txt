# Test dependencies: the whole suite on your machine, and in CI.
#
#     pip install -r requirements-test.txt
#     pytest
#
# PySpark costs several hundred MB and needs Java 17 or newer. The Spark
# transform tests also run in the container, which needs neither:
#
#     make test
#
# Verified on PySpark 4.1.3 (Java 21). The container runs the same version.

-r requirements.txt
# Pinned to the series the Spark container runs, so a test passing here means
# it passes there.
pyspark>=4.1.3,<4.2
pytest>=8.0
pytest-cov>=5.0
mongomock>=4.1        # the API and dashboard tests, with no MongoDB running
# Starlette's TestClient (used by fastapi.testclient) refuses to import
# without it; the older `httpx` is deprecated for this.
httpx2>=2.0
