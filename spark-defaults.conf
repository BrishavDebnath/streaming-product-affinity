# No spark.mongodb.* settings: writes use pymongo in foreachPartition, not
# the MongoDB Spark connector. See Dockerfile.spark.
# Shuffle partitions and the state store are set in src/streaming/job.py from
# SHUFFLE_PARTITIONS and STATE_STORE.
spark.sql.session.timeZone           UTC
spark.ui.showConsoleProgress         false
